EduPage — Backend (Foundation)
Этот репозиторий содержит базовую структуру и начальные настройки бэкенд-части проекта EduPage. На данном этапе реализован фундамент приложения и работа с пользователями.

🛠 Технологический стек
Java: версия 25.

Framework: Spring Boot 3.x.

Database: PostgreSQL.

ORM: Spring Data JPA.

Security: Spring Security (базовая конфигурация).

📂 Текущая структура и реализация
1. Архитектура пакетов
В проекте настроена стандартная многослойная структура в пакете com.edupage:

com.edupage.model — сущности (Entity).

com.edupage.repository — интерфейсы доступа к данным.

com.edupage.config, com.edupage.controller, com.edupage.security, com.edupage.service — подготовленные слои для логики приложения.

2. Модель данных (User)
Реализована базовая система пользователей:

User: поля id, name, email (уникальный), password, role.

Role: перечисление (Enum) с ролями ADMIN, TEACHER, STUDENT.

UserRepository: интерфейс для CRUD-операций в таблице users.

🚀 Инструкция по развертыванию
1. Настройка базы данных
Приложение ожидает наличие локальной базы данных PostgreSQL.

Параметры подключения в application.yml:

YAML

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/edupage
    username: postgres
    password: postgres
2. Запуск проекта
Создайте в PostgreSQL пустую БД с именем edupage.

Запустите приложение через основной класс EdupageApplication.java.

Таблицы будут созданы автоматически благодаря настройке ddl-auto: update.

Сервер будет доступен по адресу: http://localhost:8080.
