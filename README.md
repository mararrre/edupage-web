EduPage — Backend (Foundation)
Этот репозиторий содержит базовую структуру и настройки базы данных для проекта EduPage.

🛠 Что реализовано (Задача Алексея):
1. Окружение и зависимости
   Проект инициализирован на Java 25 и Spring Boot.

Подключены и настроены основные модули: Spring Data JPA, Web, Security и драйвер PostgreSQL.

2. Структура проекта
   Создана строго фиксированная структура пакетов согласно Контракту проекта:

com.edupage.model — сущности данных.

com.edupage.repository — интерфейсы для работы с БД.

com.edupage.config, com.edupage.controller, com.edupage.security, com.edupage.service — подготовлены для работы других участников команды.

3. Модель данных (Entity)
   Реализована сущность User со следующими полями: id, name, email, password, role.

Реализован Enum Role с ролями: ADMIN, TEACHER, STUDENT.

Реализован UserRepository для базовых операций с пользователями в БД.

 Инструкция по запуску

1. Требования
   Java SDK: версия 25.

Database: PostgreSQL.

2. Настройка базы данных
   Создайте в PostgreSQL пустую базу данных с именем edupage.

Откройте файл src/main/resources/application.yml.

Укажите ваши данные для подключения (логин и пароль от PostgreSQL) в секции datasource.

3. Запуск
   При первом запуске приложения Hibernate автоматически создаст таблицу users в вашей базе данных на основе реализованной модели.